{
  "name": "sprunk-empty",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate-uml": "node ./doc/generator.js",
    "generate-doc": "npx typedoc --excludeInternal false --entryPointStrategy expand --out ./doc/site/build ./src/"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "typedoc": "^0.28.1"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2",
    "sprunk-engine": "^0.4.5"
  }
}
